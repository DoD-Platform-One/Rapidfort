{{- if and (not .Values.mysql.enabled) .Values.mysql.seedDatabase }}
apiVersion: batch/v1
kind: Job
metadata:
  name: external-db-seed
  annotations:
    "helm.sh/hook-weight": "-4"
    "helm.sh/hook": pre-install
    "helm.sh/hook-delete-policy": before-hook-creation
spec:
  template:
    spec:
      imagePullSecrets:
      {{- range .Values.mysql.image.pullSecrets }}
      {{- printf "- name: %s" . | nindent 8 }}
      {{- end }}
      containers:
      - name: external-mysql-seed
        image: "{{ .Values.mysql.image.registry }}/{{ .Values.mysql.image.repository }}:{{ .Values.mysql.image.tag }}"
        command:
          - "/bin/sh"
          - "-c"
          - |
            mysql --port={{ .Values.rf_global.db.auth.port }} --host={{ .Values.rf_global.db.auth.host }} --user={{ .Values.rf_global.db.auth.username }} --password={{ .Values.rf_global.db.auth.password }} < /data/init.sql
            x=$(echo $?); curl -fsI -X POST http://localhost:15020/quitquitquit && exit $x
        volumeMounts:
        - name: init-config
          mountPath: /data
      volumes:
      - name: init-config
        configMap:
          name: init-db-configuration
      restartPolicy: Never
{{- end}}
