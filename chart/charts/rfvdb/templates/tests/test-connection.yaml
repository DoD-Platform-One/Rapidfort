apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rfvdb.fullname" . }}-test-connection"
  labels:
    {{- include "rfvdb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rfvdb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
