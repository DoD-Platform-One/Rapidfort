apiVersion: v1
kind: Service
metadata:
  name: {{ include "rfvdb.fullname" . }}
  labels:
    {{- include "rfvdb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rfvdb.selectorLabels" . | nindent 4 }}
